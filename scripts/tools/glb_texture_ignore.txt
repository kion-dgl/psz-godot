# GLBs allowed to reference a missing sibling texture, skipped by
# check_glb_textures.py. Repo-relative GLB path, one per line; '#' comments.
#
# Empty: the #245 basic/ weapon GLBs now have their sibling textures co-located
# and shipped in the pack, so the guard runs fully strict. Add an entry here
# only as a temporary allowance for a known-pending texture fix.
